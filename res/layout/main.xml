<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
        >
    <EditText
            android:id="@id/task_name"
            android:layout_width="fill_parent"
            android:layout_marginBottom="20dip"
            android:layout_height="wrap_content"
            android:text="@string/taskName"
            android:textSize="5sp"
            />
    <TableLayout android:layout_width="fill_parent" android:layout_height="wrap_content">
        <TableRow>
            <TextView android:layout_height="wrap_content"
                      android:layout_width="fill_parent"
                      android:text="@string/spend_time"
                      android:textAppearance="@style/dateTime"
                      android:layout_marginBottom="20dip"
                      android:focusable="true" android:focusableInTouchMode="true"/>
            <EditText android:id="@id/spent_time" android:numeric="integer" android:maxLength="2"/>
        </TableRow>
    </TableLayout>

    <Button android:id="@id/create_task_button" android:text="@string/create_task_submit_text"
            android:layout_width="fill_parent" android:layout_height="wrap_content"/>
</LinearLayout>
